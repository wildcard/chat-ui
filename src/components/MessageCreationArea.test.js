// Area will be fixed to the bottom of the screen.
  // b. Text field for username.
  //  i. Should be persistent across browser refreshes.
  // c. Textarea for text.
  //  i. Should be cleared after each message is sent.
  // d. Send button to send text messages.
  // e. An avatar image
  //  i. Each user will randomly receive one of 5 avatar images.
  //  ii. Avatar images should be persistent across browser refreshes
  // f. Users can't send empty messages

describe('MessageCreationArea', () => {
  describe('Text field for username.', () => {
    it('Should be persistent across browser refreshes.', () => {

    });
  });

  describe('Textarea for text.', () => {
    it('Should be cleared after each message is sent.', () => {

    });
  });

  it('has send button to send text messages.', () => {

  });

  describe('An avatar image', () => {
    test('Each user will randomly receive one of 5 avatar images.', () => {

    })

    test('Avatar images should be persistent across browser refreshes', () => {

    });
  });

  test('Users can\'t send empty messages', () => {

  });
});
